{
  "name": "@nifty-league/subgraph",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "create-local": "graph create nifty-league --node http://localhost:8020/",
    "remove-local": "graph remove nifty-league --node http://localhost:8020/",
    "deploy-local": "graph deploy nifty-league --node http://localhost:8020/ --ipfs http://localhost:5001",
    "deploy": "graph deploy nifty-league"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.20.0",
    "@graphprotocol/graph-ts": "^0.20.0"
  },
  "devDependencies": {
    "mustache": "^4.2.0"
  }
}
